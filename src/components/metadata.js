import React from 'react';
import { Helmet } from 'react-helmet';

const Metadata = () => {
	return <Helmet titleTemplate="%s | Margrit Spear" defaultTitle="Margrit Spear" />;
};

export default Metadata;
